<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="belghali.farchi.firebaseecommerce">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FireBaseEcommerce">
        <activity
            android:name=".DrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/Theme.FireBaseEcommerce.NoActionBar"></activity>
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SingleFacture" />
        <activity android:name=".Facture_List" />
        <activity android:name=".SingleProduct" />
        <activity android:name=".Products_List" />
        <activity android:name=".Facture_Create" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>